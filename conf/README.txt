This directory is parsed as an ansible host_vars location for
the "localhost" hostname.

Parsed files should end in one of:
- ".yaml"
- ".yml"
- ".json"

Files without extensions will be considered YAML.
